# Keep the sdist to the Python packages + metadata. Exclude the bundled n2p2
# source (huge, has symlinks) and the example run tree — they are not part of
# the installable package.
prune n2p2-v2.1.3-committee-nnp-extpot
prune example
prune src/QMML.egg-info
global-exclude __pycache__ *.pyc *.pyo
